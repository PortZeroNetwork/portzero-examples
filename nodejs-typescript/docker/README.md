# Node.js TypeScript docker example

```sh
PZ_TUNNEL="nodejs-typescript-docker.portzero.local:80" docker compose up --build
```

PowerShell:

```powershell
$env:PZ_TUNNEL="nodejs-typescript-docker.portzero.local:80"; docker compose up --build
```
